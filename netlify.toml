[build]
  base = "reservease-frontends"
  command = "pnpm install && pnpm run build:tenant"
  publish = "apps/tenant-portal/dist"

[build.environment]
  NODE_VERSION = "20"

[dev]
  framework = "vite"
