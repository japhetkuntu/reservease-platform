[build]
  command = "cd reservease-frontends && npm install && npm run build:tenant"
  publish = "reservease-frontends/apps/tenant-portal/dist"

[build.environment]
  NODE_VERSION = "20"

[dev]
  framework = "vite"
