[build]
  command = "cd reservease-frontends && pnpm install && pnpm run build:tenant"
  publish = "reservease-frontends/apps/tenant-portal/dist"

[build.environment]
  NODE_VERSION = "20"

[dev]
  framework = "vite"
